# ===== PRODUCTION OVERRIDES =====
# Activate with: java -jar app.jar --spring.profiles.active=prod
# Or set environment variable: SPRING_PROFILES_ACTIVE=prod

# ===== SERVER =====
server.port=8080

# ===== DATABASE (freesqldatabase.com cloud) =====
spring.datasource.url=jdbc:mysql://sql12.freesqldatabase.com:3306/sql12822250?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=sql12822250
spring.datasource.password=63Y3S1gfZu
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false
spring.jpa.show-sql=false

# ===== LOGGING (reduce verbosity in production) =====
logging.level.J2EE.PawVerse=INFO
logging.level.org.springframework.security=WARN
logging.level.org.hibernate.SQL=WARN
logging.level.org.springframework.security.config.annotation.authentication.configuration.InitializeUserDetailsBeanManagerConfigurer=ERROR

# ===== SWAGGER (disable in production) =====
springdoc.swagger-ui.enabled=false
springdoc.api-docs.enabled=false

# ===== CORS (restrict to your VPS domain — replace with actual domain) =====
# Set ALLOWED_ORIGINS env variable on VPS, e.g.: http://yourdomain.com,https://yourdomain.com
# This is read in SecurityConfig via @Value if you configure it that way.
# As a fallback, hardcode your domain below:
# cors.allowed-origins=https://yourdomain.com

# ===== MULTIPART =====
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=50MB

# ===== JWT (use strong secret in production) =====
# Override: jwt.secret=${JWT_SECRET}
# Generate with: openssl rand -base64 64

# ===== THYMELEAF =====
spring.thymeleaf.cache=true
spring.thymeleaf.check-template-location=false

# ===== FRONTEND URL =====
app.frontend-url=http://pawverse.lol

# ===== CORS =====
cors.allowed-origins=http://pawverse.lol,http://42.96.15.44

# ===== OAUTH2 - GOOGLE (production) =====
spring.security.oauth2.client.registration.google.redirect-uri=http://pawverse.lol/login/oauth2/code/google

# ===== OAUTH2 - GITHUB (production) =====
spring.security.oauth2.client.registration.github.redirect-uri=http://pawverse.lol/login/oauth2/code/github

# ===== OAUTH2 - DISCORD (production) =====
spring.security.oauth2.client.registration.discord.redirect-uri=http://pawverse.lol/login/oauth2/code/discord
